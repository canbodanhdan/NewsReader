Role of HistoryFragment:
- User Interface: Display list of articles saved in user's history through RecyclerView.
- Data linking: Get article list from database (Room) through HistoryManager and display them.

Role of HistoryManager:
- History data management: Handle adding articles to history, check if article already exists.
- Database connection: Use DAO (Data Access Object) to read/write history data in Article table.